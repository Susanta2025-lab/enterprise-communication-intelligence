# Request/Response Models

This reference is generated by inspecting the actual Pydantic v2 models in the repository:

- `app/domain/enums.py`
- `app/domain/models/message.py`
- `app/domain/models/analysis.py`
- `app/domain/schemas/analysis.py`
- `app/schemas/analysis.py`
- `app/schemas/workflow.py`
- `app/schemas/oauth.py`
- `app/schemas/mailbox.py`

All models below use `model_config = ConfigDict(extra="forbid")`, so unrecognized fields are rejected with a `422` validation error.

## Enums

### `SourceType` (`app/domain/enums.py`)

String enum identifying the origin channel of a communication.

| Value | Notes |
|---|---|
| `email` | Current MVP channel |
| `teams` | Reserved for future channel adapters |
| `slack` | Reserved for future channel adapters |
| `whatsapp` | Reserved for future channel adapters |
| `crm` | Reserved for future channel adapters |
| `erp` | Reserved for future channel adapters |
| `document` | Reserved for future channel adapters |
| `calendar` | Reserved for future channel adapters |
| `other` | Fallback/unclassified |

Only `email` is exercised by any implemented behavior; the remaining values exist so the domain model does not need to change when future channels are added.

### `PriorityLevel` (`app/domain/enums.py`)

| Value |
|---|
| `low` |
| `medium` |
| `high` |
| `critical` |

### `MessageCategory` (`app/domain/enums.py`)

| Value |
|---|
| `general` |
| `request` |
| `incident` |
| `approval` |
| `notification` |
| `inquiry` |
| `other` |

### `WorkflowActionType` (`app/domain/enums.py`)

| Value | Notes |
|---|---|
| `reply` | Phase 11 supports `REPLY` only |

### `WorkflowActionStatus` (`app/domain/enums.py`)

| Value | Notes |
|---|---|
| `pending` | Proposal exists; not authorized |
| `approved` | User authorized the stored snapshot; not sent |
| `rejected` | User denied execution; terminal in Phase 11 |
| `executing` | Authorized execution attempt has begun; after TX1 this is also the conservative marker that the external outcome is not known to ECI |
| `executed` | Provider accepted the send and the outcome was stored |
| `failed` | Provider definitely rejected the send and that outcome was stored |

---

## `MessageMetadata` (`app/domain/models/message.py`)

Channel-neutral metadata describing a communication.

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `source_type` | `SourceType` | Yes | Origin channel of the message | Must be a valid `SourceType` value |
| `sender` | `str` | Yes | Identity of the sender | `min_length=1`; trimmed and rejected if blank |
| `recipients` | `list[str]` | No (default `[]`) | Recipient identities | Each entry rejected if blank after trimming |
| `subject` | `str \| None` | No (default `None`) | Optional subject/title | If provided, rejected if blank after trimming |
| `source_id` | `str \| None` | No (default `None`) | Opaque identifier from the source system | None |
| `thread_id` | `str \| None` | No (default `None`) | Conversation/thread identifier | None |
| `sent_at` | `datetime \| None` | No (default `None`) | When the message was sent | Standard Pydantic `datetime` parsing |
| `received_at` | `datetime \| None` | No (default `None`) | When the message was received | Standard Pydantic `datetime` parsing |
| `labels` | `list[str]` | No (default `[]`) | Free-form labels/tags | Each entry rejected if blank after trimming |

## `CommunicationMessage` (`app/domain/models/message.py`)

The business communication being analyzed.

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `body` | `str` | Yes | Full text content of the communication | `min_length=1`; trimmed and rejected if blank |
| `metadata` | `MessageMetadata` | Yes | Channel-neutral metadata | Nested model validation |
| `message_id` | `str \| None` | No (default `None`) | Caller-supplied identifier for correlation | If provided, rejected if blank after trimming |

## `Summary` (`app/domain/models/analysis.py`)

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `text` | `str` | Yes | Concise summary text | `min_length=1`; trimmed and rejected if blank |
| `confidence` | `float \| None` | No (default `None`) | Optional confidence score | Must be between `0.0` and `1.0` inclusive if provided |

## `Priority` (`app/domain/models/analysis.py`)

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `level` | `PriorityLevel` | Yes | Priority classification | Must be a valid `PriorityLevel` value |
| `rationale` | `str \| None` | No (default `None`) | Explanation for the assigned priority | If provided, rejected if blank after trimming |
| `confidence` | `float \| None` | No (default `None`) | Optional confidence score | Must be between `0.0` and `1.0` inclusive if provided |

## `ActionItem` (`app/domain/models/analysis.py`)

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `description` | `str` | Yes | What needs to be done | `min_length=1`; trimmed and rejected if blank |
| `owner` | `str \| None` | No (default `None`) | Who is responsible | If provided, rejected if blank after trimming |
| `due_at` | `datetime \| None` | No (default `None`) | When the action is due | Standard Pydantic `datetime` parsing |
| `priority` | `PriorityLevel \| None` | No (default `None`) | Priority of this specific action item | Must be a valid `PriorityLevel` value if provided |

## `DraftReply` (`app/domain/models/analysis.py`)

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `body` | `str` | Yes | Suggested reply text | `min_length=1`; trimmed and rejected if blank |
| `tone` | `str \| None` | No (default `None`) | Descriptor of the reply's tone (e.g. `"neutral"`) | If provided, rejected if blank after trimming |
| `confidence` | `float \| None` | No (default `None`) | Optional confidence score | Must be between `0.0` and `1.0` inclusive if provided |

## `CommunicationAnalysis` (`app/domain/models/analysis.py`)

The structured analysis produced for a single message.

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `summary` | `Summary` | Yes | Concise summary | Nested model validation |
| `priority` | `Priority` | Yes | Priority classification | Nested model validation |
| `category` | `MessageCategory` | No (default `MessageCategory.GENERAL`) | Classified intent of the communication | Must be a valid `MessageCategory` value |
| `action_items` | `list[ActionItem]` | No (default `[]`) | Zero or more extracted action items | Each entry validated as `ActionItem` |
| `draft_reply` | `DraftReply \| None` | No (default `None`) | Suggested reply, if requested and applicable | Nested model validation if present |
| `message_id` | `str \| None` | No (default `None`) | Echoes the analyzed message's identifier | If provided, rejected if blank after trimming |

## `CommunicationRequest` (`app/domain/schemas/analysis.py`)

The request body accepted by `POST /api/v1/communications/analyze`.

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `message` | `CommunicationMessage` | Yes | The communication to analyze | Nested model validation |
| `include_draft_reply` | `bool` | No (default `True`) | Whether the provider should attempt to produce a draft reply | Standard `bool` validation |
| `include_action_items` | `bool` | No (default `True`) | Whether the provider should attempt to extract action items | Standard `bool` validation |

## `CommunicationAnalysisResult` (`app/domain/schemas/analysis.py`)

The AI-layer output produced by `CommunicationAnalysisService` / `AIProvider`.

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `analysis` | `CommunicationAnalysis` | Yes | The structured analysis result | Nested model validation |
| `provider` | `str \| None` | No (default `None`) | Opaque identifier of the provider that produced the analysis (e.g. `"mock"`) | None beyond `str \| None` typing |

## `CommunicationAnalysisResponse` (`app/schemas/analysis.py`)

The HTTP response returned by `POST /api/v1/communications/analyze`. Extends `CommunicationAnalysisResult`.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `analysis` | `CommunicationAnalysis` | Yes | The structured analysis result |
| `provider` | `str \| None` | No | Opaque provider identifier |
| `analysis_id` | `UUID \| None` | No | Persisted analysis resource id when history storage succeeded |

`analysis_id` is omitted from the JSON body when it is `null`. Persistence disabled, unauthenticated development, or a post-inference save failure all omit this field.

## `AnalysisHistoryItem` (`app/schemas/analysis.py`)

Owned history resource. Does not include raw communication body, sender, recipients, subject, email, or identity claims.

| Field | Type | Meaning |
|---|---|---|
| `analysis_id` | `UUID` | Persisted resource id |
| `created_at` | `datetime` | When the row was stored |
| `request_id` | `UUID \| None` | Operational correlation id, if stored |
| `provider` | `str \| None` | Opaque provider identifier |
| `source_type` | `SourceType` | Origin channel |
| `message_id` | `str \| None` | Caller-supplied business message id |
| `summary` | `Summary` | Stored summary |
| `priority` | `Priority` | Stored priority (`rationale` is not persisted) |
| `category` | `MessageCategory` | Stored category |
| `action_items` | `list[ActionItem]` | Stored action items |
| `draft_reply` | `DraftReply \| None` | Stored draft reply |

## `AnalysisHistoryListResponse` (`app/schemas/analysis.py`)

| Field | Type | Meaning |
|---|---|---|
| `items` | `list[AnalysisHistoryItem]` | Bounded page, newest first |
| `limit` | `int` | Page size |
| `offset` | `int` | Page offset |

Total count is omitted in Phase 9.

## `WorkflowActionCreateRequest` (`app/schemas/workflow.py`)

HTTP create body for `POST /api/v1/workflow-actions`. Extra fields are forbidden.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `analysis_id` | `UUID` | Yes | Owned analysis whose current draft reply is snapshotted |

Callers cannot supply `action_type`, `status`, `proposed_reply_body`, `approved_reply_body`, ownership, or timestamps.

## `WorkflowActionResponse` (`app/schemas/workflow.py`)

Owned workflow-action resource. Mapped explicitly from the domain `WorkflowAction`; `owner_user_id` is omitted.

| Field | Type | Meaning |
|---|---|---|
| `id` | `UUID` | Workflow action id |
| `action_type` | `WorkflowActionType` | Serialized as `"reply"` |
| `analysis_id` | `UUID` | Provenance; not dereferenced |
| `status` | `WorkflowActionStatus` | Serialized lowercase (`pending`, `approved`, ...) |
| `proposed_reply_body` | `str` | Snapshot taken at create |
| `approved_reply_body` | `str \| None` | Snapshot taken at approve; `null` until then |
| `created_at` | `datetime` | Timezone-aware ISO-8601 |
| `approved_at` | `datetime \| None` | Set on approve |
| `rejected_at` | `datetime \| None` | Set on reject |
| `executed_at` | `datetime \| None` | Set when status is `executed` |
| `failed_at` | `datetime \| None` | Set when status is `failed` |
| `has_execution_target` | `bool` | `true` when mailbox routing provenance exists (`connector_account_id` and `provider_message_id` both present). This is not a claim that the current account can send. |

The response omits `owner_user_id`, `connector_account_id`, `provider_message_id`, `credential_ref`, tokens, mailbox address, recipient, and provider response bodies.

## `WorkflowActionListResponse` (`app/schemas/workflow.py`)

| Field | Type | Meaning |
|---|---|---|
| `items` | `list[WorkflowActionResponse]` | Bounded page, newest first |
| `limit` | `int` | Page size |
| `offset` | `int` | Page offset |

Total count is omitted.

## `GmailAuthorizationStartResponse` (`app/schemas/oauth.py`)

Returned by `POST /api/v1/connector-accounts/gmail/authorize`. Extra fields are forbidden.

| Field | Type | Meaning |
|---|---|---|
| `authorization_url` | `str` | Google authorization URL. Includes Phase 13A `state` and PKCE challenge; the raw state and verifier are not separate response fields. |
| `expires_at` | `datetime` | Session expiry |

The response omits `state`, PKCE verifier, redirect URI, client secret, `credential_ref`, and tokens.

## `GmailAuthorizationCallbackResponse` (`app/schemas/oauth.py`)

Returned by `GET /api/v1/oauth/callbacks/gmail` after a successful Google redirect.

| Field | Type | Meaning |
|---|---|---|
| `provider` | `str` | Always `gmail` |
| `connector_account_id` | `UUID` | Owned connector-account id |
| `external_account_id` | `str` | Verified Google OIDC `sub`, never email |
| `status` | `ConnectorAccountStatus` | Serialized lowercase (`active`, ...) |
| `granted_capabilities` | `tuple[CommunicationCapability, ...]` | Explicit grants from accepted Gmail scopes |

The response omits tokens, refresh tokens, ID tokens, `credential_ref`, client secret, and PKCE material.

## `MicrosoftAuthorizationStartResponse` (`app/schemas/oauth.py`)

Returned by `POST /api/v1/connector-accounts/microsoft_graph/authorize`. Extra fields are forbidden.

| Field | Type | Meaning |
|---|---|---|
| `authorization_url` | `str` | Microsoft identity platform v2 authorization URL. Includes Phase 13A `state` and PKCE challenge; the raw state and verifier are not separate response fields. |
| `expires_at` | `datetime` | Session expiry |

The response omits `state`, PKCE verifier, redirect URI, client secret, `credential_ref`, and tokens.

## `MicrosoftAuthorizationCallbackResponse` (`app/schemas/oauth.py`)

Returned by `GET /api/v1/oauth/callbacks/microsoft_graph` after a successful Microsoft redirect.

| Field | Type | Meaning |
|---|---|---|
| `provider` | `str` | Always `microsoft_graph` |
| `connector_account_id` | `UUID` | Owned connector-account id |
| `external_account_id` | `str` | Verified Microsoft `{tid}:{oid}`, never email |
| `status` | `ConnectorAccountStatus` | Serialized lowercase (`active`, ...) |
| `granted_capabilities` | `tuple[CommunicationCapability, ...]` | Explicit grants from accepted Graph scopes |

The response omits tokens, refresh tokens, ID tokens, `credential_ref`, client secret, and PKCE material.

## `ConnectorAccountResponse` (`app/schemas/oauth.py`)

Returned by `POST /api/v1/connector-accounts/{connector_account_id}/disconnect`. Extra fields are forbidden.

| Field | Type | Meaning |
|---|---|---|
| `id` | `UUID` | Connector-account id |
| `provider` | `str` | Stored provider (`gmail` or `microsoft_graph`) |
| `external_account_id` | `str` | Provider mailbox identity |
| `status` | `ConnectorAccountStatus` | After success, `disconnected` |
| `granted_capabilities` | `tuple[CommunicationCapability, ...] \| None` | After success, `null` |
| `created_at` | `datetime` | Created timestamp |
| `updated_at` | `datetime` | Updated timestamp |

The response omits `credential_ref`, tokens, user id, and provider error bodies.

## `ConnectorAccountReauthorizeResponse` (`app/schemas/oauth.py`)

Returned by `POST /api/v1/connector-accounts/{connector_account_id}/reauthorize`. Extra fields are forbidden.

| Field | Type | Meaning |
|---|---|---|
| `authorization_url` | `str` | Provider authorization URL for the account's stored provider |
| `expires_at` | `datetime` | Session expiry |

The response omits `state`, PKCE verifier, `credential_ref`, and tokens.

## `ConnectorAccountMessageListQuery` (`app/schemas/mailbox.py`)

Frozen Phase 14 list query. Extra fields are forbidden. Not served over HTTP in 14A.

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `page_size` | `int` | No (default `10`) | Bounded page size | `1` through `100` inclusive |
| `cursor` | `str \| None` | No (default `None`) | Opaque continuation token | If provided, rejected if blank after trimming. Callers must not parse it. |

## `ConnectorAccountMessageListItem` (`app/schemas/mailbox.py`)

Provider-neutral selection metadata. Extra fields are forbidden.

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `provider_message_id` | `str` | Yes | Opaque provider message identifier | Trimmed; rejected if blank |
| `sender` | `str` | Yes | Sender identity | Trimmed; rejected if blank |
| `subject` | `str \| None` | No (default `None`) | Optional subject | If provided, rejected if blank after trimming |
| `sent_at` | `datetime \| None` | No (default `None`) | Normalized sent timestamp when available | Standard Pydantic `datetime` parsing |
| `received_at` | `datetime \| None` | No (default `None`) | Normalized received timestamp when available | Standard Pydantic `datetime` parsing |

The item does not include body, attachments, `thread_id`, `credential_ref`, tokens, or provider pagination URLs.

## `ConnectorAccountMessageListResponse` (`app/schemas/mailbox.py`)

| Field | Type | Meaning |
|---|---|---|
| `items` | `list[ConnectorAccountMessageListItem]` | Current page |
| `next_cursor` | `str \| None` | Opaque continuation token, or `null` on the last page |

`next_cursor` is transport data. It is not a Graph `nextLink` or Gmail page-token field name.

## `ConnectorAccountMessageAnalyzeRequest` (`app/schemas/mailbox.py`)

Frozen Phase 14 mailbox-backed analyze body. Extra fields are forbidden. `provider_message_id` is a JSON field, not a path segment.

| Field | Type | Required | Meaning | Validation |
|---|---|---|---|---|
| `provider_message_id` | `str` | Yes | Opaque provider message identifier | Trimmed; rejected if blank |

The mailbox-backed analyze response reuses `CommunicationAnalysisResponse`. It does not add raw message body, `credential_ref`, or token fields.

## API-Level Schemas (not domain schemas)

`app/schemas/` (distinct from `app/domain/schemas/`) holds transport-only response models used by the health endpoints:

- `LivenessResponse` — `status: str`
- `HealthResponse` — `status: str`, `service: str`, `version: str`, `environment: str`
- `ReadinessResponse` — `status: str`
- `CommunicationAnalysisResponse` — analyze HTTP body, including optional `analysis_id`
- `AnalysisHistoryItem` / `AnalysisHistoryListResponse` — owned history
- `WorkflowActionCreateRequest` / `WorkflowActionResponse` / `WorkflowActionListResponse` — workflow proposal, approval, and execute
- `GmailAuthorizationStartResponse` / `GmailAuthorizationCallbackResponse` — Gmail mailbox OAuth start and Google callback
- `MicrosoftAuthorizationStartResponse` / `MicrosoftAuthorizationCallbackResponse` — Microsoft mailbox OAuth start and callback
- `ConnectorAccountResponse` / `ConnectorAccountReauthorizeResponse` — disconnect metadata and reauthorize start
- `ConnectorAccountMessageListQuery` / `ConnectorAccountMessageListItem` / `ConnectorAccountMessageListResponse` / `ConnectorAccountMessageAnalyzeRequest` — Phase 14 mailbox list/analyze contract (not served in 14A)
- `ErrorResponse` (`app/schemas/errors.py`) — `detail: str`; used only for OpenAPI documentation of error responses (see [Error Handling](error-handling.md))

These are not reused as domain models; they exist purely to describe the shape of the health/readiness/error responses in OpenAPI.
