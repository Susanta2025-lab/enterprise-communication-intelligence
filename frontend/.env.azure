# Public build-time presentation metadata only.
# Not credentials, not authorization inputs, not infrastructure identifiers.
# Loaded by: npm run build:azure  (equivalent: npm run build -- --mode azure)
# Precedence: .env < .env.local < .env.azure < .env.azure.local < shell

VITE_ECI_CLOUD_PROVIDER=azure
VITE_ECI_AI_PROVIDER=microsoft_foundry
VITE_ECI_CLOUD_REGION=Spain Central
