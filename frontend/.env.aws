# Public build-time presentation metadata only.
# Not credentials, not authorization inputs, not infrastructure identifiers.
# Loaded by: npm run build:aws  (equivalent: npm run build -- --mode aws)
# Precedence: .env < .env.local < .env.aws < .env.aws.local < shell

VITE_ECI_CLOUD_PROVIDER=aws
VITE_ECI_AI_PROVIDER=amazon_bedrock
VITE_ECI_CLOUD_REGION=eu-south-2
